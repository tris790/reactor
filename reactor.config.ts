/**
 * Reactor Translation Debugger Configuration
 *
 * This file defines paths for the translation debugger.
 * You can move your frontend code anywhere and update these paths.
 */

export default {
  // Directory containing your React components (can be relative or absolute path)
  sourceDir: "demo/src",

  // Directory containing translation JSON files (can be relative or absolute path)
  translationsDir: "demo/src/translations",

  // Supported locales (language codes matching your translation files)
  locales: ["en", "fr"],
};
